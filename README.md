# ReverseMap
'Python based dictionary that can be utilized to search key value pairs with the contains kjeyword over both the keys and the values. It can also be used on values that are not hashable by converting them a Convertible. The Convertible class can be reverted back to the original value from the hashable key form'
